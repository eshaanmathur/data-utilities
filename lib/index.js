const unique = require('./unique');
const count = require('./count');
const rank = require('./rank');
const parseCsv = require('./parseCsv');
const parseSheet = require('./parseSheet');
const reportToTable = require('./reportToTable');
const tableToSheet = require('./tableToSheet');
const percentage = require('./percentage');
const tableToReport = require('./tableToReport');

module.exports = {
  unique,
  count,
  rank,
  parseCsv,
  parseSheet,
  reportToTable,
  tableToSheet,
  percentage,
  tableToReport,
};
